Add the following to the `Upgrade Spring Boot Parent Version` section:

```editor:open-file
file: ~/exercises/upgrade-notes.md
```

```markdown
- moved hardcoded `itextpdf` version to <properties>
```

